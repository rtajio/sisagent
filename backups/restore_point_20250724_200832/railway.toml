[build]
builder = "nixpacks"

[deploy]
startCommand = "python init_db.py && gunicorn wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10 